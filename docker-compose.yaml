version: '3.8'

services:
  # MongoDB Servisi (Konteyner içi erişim adı: mongodb)
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # Redis Servisi
  redis_kapsayici:
    image: redis:alpine
    container_name: redis_kapsayici
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

  # RabbitMQ Servisi
  rabbitmq_kapsayici:
    image: rabbitmq:3-management-alpine
    container_name: rabbitmq_kapsayici
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Backend API Servisi
  sahaapp_api:
    build:
      context: ./rest-api
    container_name: sahaapp_api
    ports:
      - "9000:9000"
    environment:
      - MONGO_URI=mongodb://mongodb:27017/SahaAppDB
      - PORT=9000
      - REDIS_URL=redis://redis_kapsayici:6379
      - RABBITMQ_URL=amqp://rabbitmq_kapsayici
    depends_on:
      mongodb:
        condition: service_healthy
      redis_kapsayici:
        condition: service_healthy
      rabbitmq_kapsayici:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"const http = require('http'); http.get('http://localhost:9000/api/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1); }).on('error', () => process.exit(1));\""]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Frontend Web Servisi (Geçici Olarak Devre Dışı Bırakıldı)
  # sahaapp_web:
  #   build:
  #     context: ./saha-app-web
  #   container_name: sahaapp_web
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - sahaapp_api

volumes:
  mongodb_data:
